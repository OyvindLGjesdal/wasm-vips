name: Emscripten CI

on: [push]

jobs:
  build:
    runs_on: ubuntu-latest

    steps:
    - name: clone emsdk
      run:  git clone https://github.com/emscripten-core/emsdk.git
    - name: list working directory
      run: ls && ./emsdk install latest
      working_directory: $GITHUB_WORKSPACE/emsdk
    - name: install, activate and source
      run: ./emsdk install latest && ./emsdk activate latest && source ./emsdk_env.sh 
      working_directory: $GITHUB_WORKSPACE/emdsk
 
